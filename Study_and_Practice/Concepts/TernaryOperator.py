# What is the ternary operation in Python, similar to ?: in Java?
# In Python we have:
# [if_true] if [expression] else [if_false]
age = 25
status = "married" if age > 30 else "unmarried"
print(status)
