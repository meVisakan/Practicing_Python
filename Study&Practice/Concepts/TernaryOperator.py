# What is the ternary operation in Python, similar to ?: in Java? but in python we have

# [if_true] if [expression] else [if_false]
age = 25
status = "married" if age > 30 else "unmarried"
print(status)
